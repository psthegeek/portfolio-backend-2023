{
  "name": "portfoliopro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
     "build": "cd . && npm install && cd client && npm install && npm run build ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run  server\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.5.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.18.2",
    "framer-motion": "^10.17.9",
    "nodemailer": "^6.9.8",
    "nodemon": "^2.0.12"
  }
}
